@model B2B.Models.KNA1

<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
    <div class="navbar-inner">
        <div class="container">
            <a class="brand pull-left text-center" href="#">
                <img src="~/alfemo.png" alt="logo.svg (43 KB)" width="85%" style="border-radius:3px">
            </a>

            <ul class="nav navbar-nav user_menu pull-right">
                <li class="divider-vertical hidden-sm hidden-xs"></li>
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle cursor-default">
                        <img src="img/user_avatar.png" alt="" class="user_avatar">
                        @if (Model.SORTL.Equals("MUSTERI"))
                        {
                            @Model.NAME1
                        }
                        else
                        {
                            @(Model.KUNNR.Substring(3)+"-"+Model.NAME1)
                        }
                    </a>
                </li>
            </ul>
        </div>
    </div>
</nav>

